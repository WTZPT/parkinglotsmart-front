import React, { Component } from "react";
import PropTypes from "prop-types";

export default class user extends Component {
  static propTypes = {
    prop: PropTypes,
  };

  render() {
    return <div>个人用户页</div>;
  }
}
